[Credentials]
username =
password =
      - تشغيل : pip install bandit black codespell flake8 isort mypy pytest pyupgrade safety
      - تشغيل : bandit -r . || صحيح
      - تشغيل : black --check . || true
      - تشغيل : codespell -- quiet-level=2 || true   # -- ignore-words-list="" -- skip=""
      - تشغيل : codespell --ignore-words-list=" followings " -- quiet-level=2
      - تشغيل : flake8. --count --select=E9,F63,F7,F82 --show-source --statistics
      - تشغيل : isort --check-only --profile black . || true
      - تشغيل :f63
